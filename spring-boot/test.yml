info:
  name: test
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/employee/new
  body:
    type: json
    data: |2-
        {
          "id": 102660,
          "name": "Mohamed Nagy",
          "salary": 400
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
