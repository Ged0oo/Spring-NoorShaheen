info:
  name: test
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/employee/department/10
  body:
    type: json
    data: |2-
        {
          "dno": 12,
          "name": "Managment"
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
