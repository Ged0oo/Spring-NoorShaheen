info:
  name: UpdateBook
  type: http
  seq: 1

http:
  method: PUT
  url: http://localhost:9090/books/4
  body:
    type: json
    data: |-
      {
        "author":{
          "id": 1
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
