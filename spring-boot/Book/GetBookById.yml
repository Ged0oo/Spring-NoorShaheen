info:
  name: GetBookById
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:9090/books/1
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
