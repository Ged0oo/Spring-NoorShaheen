info:
  name: AddNewAuthor
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:9090/authors
  body:
    type: json
    data: |-
      {
        "name": "Muhanad Nagy"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
