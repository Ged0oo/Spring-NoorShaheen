info:
  name: UpdateAuthor
  type: http
  seq: 1

http:
  method: PUT
  url: http://localhost:9090/authors/1
  body:
    type: json
    data: |-
      {
        "name": "Nagy"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
